# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# BT-rescore generated reports (contain student data)
bt-report-*.html

# Análisis privado del ranking acumulado — no se versiona ni se proyecta
course-standings-*.html

# Course readings and research papers (copyrighted PDFs — do not redistribute).
# Learnings extracted from literatura/evaluacion_llm/ are tracked in
# docs/evaluacion_llm_literatura.md so they survive without the PDFs.
literatura/

# Materiales crudos de los cursos: PDFs de pago, capturas, fuentes sin licencia
# para redistribuir. Este repo es publico y despliega a GitHub Pages — de aca
# solo salen extractos destilados hacia knowledge_base.md.
materials/

# Visual companion de brainstorming (superpowers)
.superpowers/
